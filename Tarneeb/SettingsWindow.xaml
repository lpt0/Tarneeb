<Window x:Class="Tarneeb.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tarneeb"
        mc:Ignorable="d"
        Loaded="OnWindowLoad"
        WindowStartupLocation="CenterScreen"
        Title="Settings" Height="300" Width="275">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="0.25*" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="StackPanel">
                <Setter Property="Margin" Value="24 24 24 0" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="Width" Value="125" />
                <Setter Property="Height" Value="35" />
            </Style>
        </Grid.Resources>

        <!-- PlayerName -->
        <StackPanel Grid.Row="0">
            <Label>
                What is your name?
            </Label>
            <TextBox x:Name="PlayerName"></TextBox>
        </StackPanel>

        <!-- MaxScore -->
        <StackPanel Grid.Row="1">
            <Label>
                What should the max score be?
            </Label>
            <TextBox x:Name="MaxScore"></TextBox>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button x:Name="Save" Grid.Column="0" Click="OnSaveClicked">Save and Exit</Button>
                <Button x:Name="Exit" Grid.Column="1" Click="OnExitClicked">Exit without Saving</Button>
            </Grid>
        </Grid>
    </Grid>
</Window>
