<Window x:Class="Tarneeb.DifficultySelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tarneeb"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"  WindowStyle="None"
        Title="Select a difficulty" Height="150" Width="225">
    <Grid HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Grid.Row="0">Select a difficulty level for this game:</Label>
        <ComboBox x:Name="cmbDifficulty" Grid.Row="1" Width="175" Height="25" SelectionChanged="OnSelection"></ComboBox>
        <Button x:Name="btnOk" Click="OnClick" Grid.Row="2" Width="75" Height="25" IsEnabled="False">OK</Button>
    </Grid>
</Window>
